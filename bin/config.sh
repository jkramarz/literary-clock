
# without titles and authors
#TIMELIT_DIR=/mnt/base-us/extensions/literary-clock/images

# with titles and authors
TIMELIT_DIR=/mnt/base-us/extensions/literary-clock/images/metadata

# both must be >=60
REFRESH_INTERVAL_CHARGING=60 
REFRESH_INTERVAL_BATTERY=900

# Whether to create log output (1) or not (0).
LOGGING=0

# Where to log to - either /dev/stderr for console output, or an absolute
# file path (beware that this may grow large over time!)
LOGFILE=/dev/stderr

# the real-time clock to use (0, 1 or 2)
RTC=0
